# Optional local paths. The scripts also work when the binaries are already in PATH.
BLENDER_BIN=
GODOT_BIN=

# Optional MCP settings for your own local experiments. Do not expose publicly.
BLENDER_MCP_HOST=127.0.0.1
BLENDER_MCP_PORT=9877
